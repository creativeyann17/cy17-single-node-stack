# cy17-single-node-stack (API)

## DevTools

It's recommended to enable **Devtools (auto reload) for IntelliJ** in settings.

- Compiler > Build project automatically
- Advanced Settings > Allow auto-make to start even if app is running
